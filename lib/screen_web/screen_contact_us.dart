import 'package:flutter/material.dart';

class WebContactUs extends StatelessWidget {
  const WebContactUs({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Text('WebContactUs'),
      ),
    );
  }
}
